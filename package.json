{
  "name": "hashlabs.github.io",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:hashlabs/hashlabs.github.io.git",
  "author": "Victor Tortolero <victormtortolero@gmail.com>",
  "license": "MIT",
  "deploy": "yarn build && firebase deploy",
  "dependencies": {
    "bootstrap": "4.0.0-alpha.6"
  },
  "scripts": {
    "serve": "jekyll serve --config _config.yml,_config.development.yml",
    "build-staging": "JEKYLL_ENV=staging jekyll build --config _config.yml,_config.staging.yml",
    "build-production": "JEKYLL_ENV=production jekyll build --config _config.yml,_config.production.yml",
    "deploy": "yarn build-staging && firebase deploy"
  }
}
